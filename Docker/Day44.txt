#Docker Compose file

services:
  web:
    image: httpd:latest
    ports: 
    - "3002:80"
    container_name: httpd
    volumes:
    - /opt/dba:/usr/local/apache2/htdocs

docker compose up -dba

curl http://localhost:3003/index1.html